# <ID> — <Experiment name>

- **Status:** ⬜ PASS / ⬜ FAIL / ⬜ BLOCKED
- **Date (UTC):**
- **Environment:** AKS `<ver>` + Premium geo-replicated ACR (region + replicas)
- **Injection mechanism:** [Client-side Chaos Mesh | NSG | Agent | Load | ACR-native | Dependency]

## Hypothesis
<what resilient behavior we expect>

## Steady-state signal (oracle)
<what "healthy" looks like and how it's measured>

## What was done (reproducible steps)
```bash
# exact commands
```

## Evidence
```
# key command output (before / during / after)
```

## Result
<PASS/FAIL/BLOCKED + one-line verdict against the hypothesis>

## Findings / gotchas
<anything learned, limitations, workarounds>

## Cleanup
<what was reverted; fault cancelled/recovered>
